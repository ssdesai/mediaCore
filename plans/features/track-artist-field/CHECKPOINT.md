# Checkpoint: track-artist-field

status: committed
updated: 2026-09-06T22:42:53Z
gate: all checks passed (5 checks: install, lint, fixture idempotent,
      wheel contains fixture, tests — 282 passed; 0 skipped)

## Slices
- [x] acceptance tests — tests/test_release.py, tests/test_bundle.py, tests/test_fixture.py,
      frozen schema-1 asset tests/assets/its-saxy-schema-1/release.json
      (12 red, expected; commit 9a77d0e)
- [x] 1. `Track.artist: str | None = None` after `title`; `SCHEMA_VERSION` 1 -> 2
- [x] 2. package version 0.2.0 -> 0.3.0 (pyproject.toml, src/mediacore/__init__.py)
- [x] 3. fixture regenerated by scripts/make_fixture_its_saxy.py, committed
- [x] 4. docs — INTEGRATION.md (§3 shape, §5 line, §6 mapping, §8/§9 consumer notes,
      §12 versioning, §13 decision 2026-09-06), README.md pins, plans/PROJECT_FACTS.md
- [x] 5. READMEs — src/mediacore/, tests/, new tests/assets/, fixtures/ if affected
- [x] 6. NOTES.md rulings; gate green; commit

## Learned
- `write_bundle` serialises `release.model_dump(mode="json")` with no `exclude_none`,
  so every optional field is a key with `null` — `artist` follows (point 4).
- The gate's "fixture idempotent" check fails on any tracked change under `fixtures/`,
  so the regenerated `release.json` must be committed exactly as the script writes it.
- `read_bundle(..., verify=False)` touches no media, so the frozen schema-1 asset is
  `release.json` alone; no media bytes are copied under `tests/`.

## Resume
- Nothing to resume: committed on `track-artist-field`, gate green, not pushed and not
  tagged. Next is the review pass (`./agentTooling/run-review.sh track-artist-field`),
  run by the coordinator, which opens the PR. The `v0.3.0` tag is the human's, after
  the merge.
