[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "mediacore"
version = "0.2.0"
description = "The shared recorded-media Release contract: schema, identity refs, name normalisation, bundle I/O, and one real-world test fixture."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Sahil Desai" }]
dependencies = ["pydantic>=2.7"]

[project.optional-dependencies]
# `s3` is what an s3:// bundle store needs (INTEGRATION.md §5.1). It stays optional: a
# consumer on a file:// store installs nothing extra, and mediacore never holds keys —
# boto3 resolves credentials from the ambient chain.
s3 = ["boto3>=1.34"]
# moto mocks S3 in-process, so the s3 backend is covered by tests that run offline in
# the gate rather than by a skipped suite.
dev = ["pytest>=8.0", "ruff>=0.6", "boto3>=1.34", "moto[s3]>=5.0"]

[project.urls]
Homepage = "https://github.com/ssdesai/mediaCore"

[tool.hatch.build.targets.wheel]
packages = ["src/mediacore"]

# The fixture bundle lives at the repo root, not inside the package, but a consumer
# that installs from a git URL still has to reach it through `its_saxy_bundle()`.
[tool.hatch.build.targets.wheel.force-include]
"fixtures" = "mediacore/_fixtures"

[tool.hatch.build.targets.sdist]
include = ["src/mediacore", "fixtures", "scripts", "tests", "README.md", "INTEGRATION.md"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.ruff.lint.isort]
known-first-party = ["mediacore"]
known-local-folder = ["conftest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
