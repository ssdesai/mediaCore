# 92 — level sentinel: the capture layer

Level 1 (plans 89–91) owns every dollar and second decision. The gate runs here so that
level 2 — `report.py` and the doctrine that describes the rule — is built on a capture
layer proved green, and so that nothing in level 2 can quietly change what a share *is*.

Must be green: bash syntax, `py_compile analysis`, and the behavioural tests, in
particular `self/tests/session-share.sh`, `self/tests/session-claims.sh`,
`self/tests/capture-guard.sh`, `self/tests/subagent-capture.sh` and
`self/tests/claims-ledger.sh`.

No `expected-red:` and no `defer:`. Plan 93's tests do not exist yet, and
`claims-ledger.sh` part B asserts no dollar figure — only that the second capture is not
refused and that the annotation names the other feature, both of which survive the share
unchanged. If this gate is red, something in level 1 is wrong.
