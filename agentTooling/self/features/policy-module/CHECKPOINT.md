# Checkpoint: policy-module

status: committed          planned | tests-written | implementing | gating | committed
updated: 2026-09-18T11:41:28Z
gate (round 1): `all checks passed` (self/gate-report.txt VERDICT, exit 0) — full tree:
      shell syntax, every self-test including the new `policy table self-test`, python
      syntax (`hooks/policy.py` on the `py_compile hooks` line), the byte-for-byte
      permission policy check, rate table, runner prerequisites; shellcheck not
      installed, no SKIPPED, no FAIL
gate (round 2): `=== gate: done — all checks passed ===` (self/gate-report.txt), exit 0 —
      same full tree, including `hook escalation self-test` with the three new scratch
      assertions

## Round 2 — the one escalated finding, decision (a)

- [x] Confirmed the defect directly against `scratch_argument_allowed` (bypassing
      `command_allowed`, which already prompts on the escalation's literal `../x`
      example for an unrelated reason — the `UNANALYSABLE` guard on any `..`): before
      the fix it returned `True` for `"../x"` and `"--out=../x"` against an absent path.
- [x] Fix: a relative, non-flag value (incl. the value after `=`) must resolve
      lexically inside the project root or the scratch root before existence or a
      symlink gets a vote (NOTES ruling 17).
- [x] `self/tests/hook-escalation.sh`: `SCRATCH_PROMPT` gains the two `../x` cases,
      `SCRATCH_ALLOW` gains `new-output.txt`; every earlier scratch expectation
      unchanged.
- [x] `hooks/README.md` § Headless runners and the scratch directory — one paragraph
      naming the lexical-first rule and why (an argument may be an output path).
- [x] NOTES.md ruling 17; this checkpoint.
- [x] `self/tests/README.md`'s `hook-escalation.sh` entry — the three new scratch
      assertions named (CONVENTIONS.md § Keeping READMEs up to date).
- [x] Gate green, one commit.

## Slices (round 1)
- [x] acceptance tests — written first and confirmed RED: `self/tests/policy-table.sh`
      (new, import error), 16 cases in `allow-repo-commands.sh` (4 bare entry points
      approved, 3 `git branch --list` denied, 2 reasons missing `feature-close.sh`, 7+2
      unreadable), 5 in `hook-escalation.sh` (scratch arguments), `hook-wiring.sh`
      (import error, then the byte-for-byte phase)
- [x] P1. `hooks/policy.py` — the table, `bash_deny_rules()`, both scripts import it by
      sibling path; `self/gate.sh` enumerates the new test and the new py_compile file
- [x] P2. `--self --check` byte-for-byte with a diff-shaped message; `--self --write`
      generates (ruling 2); `.claude/settings.json` regenerated by the writer
- [x] P3. `permissions.ask` with the any-depth `hooks/` rule; the deny for it removed;
      `ask` merged with the `deny` rules' semantics and type-checked like them
- [x] P4. the seventh opaque shape (`does_not_tokenize`, `UNREADABLE_DENY_REASON`,
      `opaque_deny_reason` as the tri-state)
- [x] P5. scratch arguments confined to the project root or the scratch root
- [x] P6. `entry_script` wants a directory component; `git branch --list <pattern>` reads
- [x] P7. `sweep.sh` gone; `GIT_DENY_REASON` names both scripts; docs; eight backlog
      entries removed, the opaque one rewritten (ruling 14)
- [x] READMEs: `hooks/`, root, `self/`, `self/tests/`, `self/PROJECT_FACTS.md`
- [x] gate green, commit

## Learned
- A merging `--write` cannot satisfy a byte-for-byte `--check` on its own output: the new
  rules append at the end instead of in canonical position. That is ruling 2, and it is
  the one design decision the build had to take rather than read.
- `grep -rn "sweep\.sh" hooks/ self/tests/allow-repo-commands.sh` (the P7 assertion) is
  clean; the only remaining hit in the repo is in other features' historical records.
- The worktree's `hooks/` was editable with the Edit tool throughout, exactly as §12
  predicted — that is the hole this feature's ask rule closes for the next session.

## Resume
- Worktree `/Users/sahildesai/dev/agentTooling/.worktrees/policy-module`, branch
  `policy-module`, base `lifecycle-records-and-numbering`.
- Tests: `bash self/tests/{policy-table,allow-repo-commands,hook-escalation,hook-wiring}.sh`.
- Gate: `bash self/gate.sh` from the worktree; every check must be `ok`.
- `.claude/settings.json` is regenerated only by
  `python3 -B hooks/wire-settings.py --self --repo <worktree> --write`, never edited; the
  gate's check compares it byte for byte.
- Next (round 1): the review pass (`./run-review.sh --self policy-module`), not run from
  here — it is what raised the round 2 finding above.
- Round 2 is committed as of this checkpoint; not run from here: `run-review.sh`,
  `feature-close.sh`, a push, or a PR.
