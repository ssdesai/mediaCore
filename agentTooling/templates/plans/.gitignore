# Generated by agentTooling/sync-plans.sh — edit templates/plans/.gitignore, not this copy.
# Gate reports and the runners' raw event streams: rewritten every batch, never committed.
gate-report*.txt
**/*.stream.jsonl
**/*.logfifo

# The review pass's verdict, rewritten every batch and used as the PR body. Anchored to
# plans/ on purpose: a feature's own archived copy (features/<slug>/review-report.md) is
# the committed record of that batch's verdict, and an unanchored pattern would swallow it.
/review-report.md
