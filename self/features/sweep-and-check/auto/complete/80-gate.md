# level 1 — the scripts (73–79) and their tests. Nothing is expected red here: the
# three tests plans assert the three scripts, plan 79's phase asserts its own change,
# and the final gate owns the same set.
